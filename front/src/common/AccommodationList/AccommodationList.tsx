import React from 'react';
import IPage from '../../interfaces/IPage';

const AccommodationList: React.FC<IPage> = () => (
  <div>
    Accommodation list
  </div>
);

export default AccommodationList;
